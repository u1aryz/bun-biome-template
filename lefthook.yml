pre-commit:
  commands:
    format:
      run: bunx oxfmt {staged_files} && bunx oxlint --fix {staged_files}
      stage_fixed: true
    typecheck:
      run: bunx tsc --noEmit

commit-msg:
  commands:
    "lint commit message":
      run: bunx --no -- commitlint --edit "$1"
