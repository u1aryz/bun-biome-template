{
	"name": "bun-starter-template",
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"start": "bun run src/index.ts",
		"dev": "bun run --watch src/index.ts",
		"format": "oxfmt . && oxlint --fix .",
		"typecheck": "tsc --noEmit",
		"test": "vitest run"
	},
	"devDependencies": {
		"@commitlint/cli": "20.3.1",
		"@commitlint/config-conventional": "20.3.1",
		"@types/bun": "latest",
		"lefthook": "2.0.15",
		"oxfmt": "^0.27.0",
		"oxlint": "^1.42.0",
		"vitest": "4.0.18"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
